generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Bookmark {
  id Int @id @default(autoincrement())
  user_id Int
  url String
  name String
  color String
  icon String
}

model Folder {
  id Int @id @default(autoincrement())
  creator_id Int
  collabs_id Int[]
  name String
}
