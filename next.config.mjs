/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "cdn.oaistatic.com",
      "blog.logrocket.com",
      "nextjs.org",
      "cdn.sanity.io",
    ],
  },
};

export default nextConfig;
